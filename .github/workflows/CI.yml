name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Unit Test Coverage
      run: |
        npm install
        npm run coverage
